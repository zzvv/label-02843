# 多阶段构建 - 构建阶段
# 使用官方 Node.js 镜像（支持 ARM64 和 AMD64）
# node:20-alpine 和 nginx:alpine 均为官方多架构镜像，自动支持跨平台
FROM node:20-alpine AS builder

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 构建生产版本
RUN npm run build

# 生产阶段 - 使用 Nginx 服务静态文件
# 使用官方 Nginx 镜像（支持 ARM64 和 AMD64）
FROM nginx:alpine

# 复制构建产物到 Nginx 默认目录
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置文件
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 启动 Nginx
CMD ["nginx", "-g", "daemon off;"]
